name: Mobile

# Builds the shared Go core into the iOS xcframework and the Android AAR on
# every change to the mobile code. This validates that mobilecore stays
# gomobile-compatible (e.g. no exported method uses an unsupported type) and
# that both native toolchains can consume it. The actual app builds
# (xcodebuild for iOS, ./gradlew for Android) are added below once the
# ios/ and android/ app projects exist — see the placeholder steps.

on:
  push:
    branches: [main]
    paths:
      - "mobilecore/**"
      - "internal/**"
      - "ios/**"
      - "android/**"
      - "go.mod"
      - "go.sum"
      - ".github/workflows/mobile.yml"
  pull_request:
    paths:
      - "mobilecore/**"
      - "internal/**"
      - "ios/**"
      - "android/**"
      - "go.mod"
      - "go.sum"
      - ".github/workflows/mobile.yml"

permissions:
  contents: read

jobs:
  ios:
    name: iOS binding (Core.xcframework)
    runs-on: macos-15
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      # gomobile compiles the mobile stdlib on a cold runner; cache it.
      - name: Cache Go build cache
        uses: actions/cache@v4
        with:
          path: ~/Library/Caches/go-build
          key: ${{ runner.os }}-gomobile-ios-${{ hashFiles('**/go.sum') }}-v1
          restore-keys: |
            ${{ runner.os }}-gomobile-ios-

      - name: Portability check (core must not import darwin-only code)
        run: CGO_ENABLED=0 GOOS=linux go build ./mobilecore

      - name: Install gomobile
        run: |
          go install golang.org/x/mobile/cmd/gomobile@latest
          go install golang.org/x/mobile/cmd/gobind@latest
          gomobile init

      - name: Build Core.xcframework
        run: |
          mkdir -p ios/Frameworks
          gomobile bind -target=ios,iossimulator \
            -o ios/Frameworks/Core.xcframework ./mobilecore
          ls -R ios/Frameworks/Core.xcframework | head -20

      - name: Upload xcframework
        uses: actions/upload-artifact@v4
        with:
          name: Core.xcframework
          path: ios/Frameworks/Core.xcframework
          if-no-files-found: error

      # Build the SwiftUI app against the freshly-built xcframework. The
      # checked-in .xcodeproj links ios/Frameworks/Core.xcframework produced
      # above. Build for the simulator SDK (no code signing) so no provisioning
      # profile or Apple account is needed on the runner.
      - name: Build iOS app
        run: |
          xcodebuild -project ios/DailyProgress.xcodeproj \
            -scheme DailyProgress \
            -sdk iphonesimulator -arch arm64 \
            CODE_SIGNING_ALLOWED=NO ONLY_ACTIVE_ARCH=NO ARCHS=arm64 \
            build
        # NOTE: XCTest run is intentionally not wired here - it requires a
        # booted simulator with a pinned device name (flaky on runners). The
        # build above proves the app compiles and links against the core;
        # unit tests run locally via xcodebuild ... test.

  android:
    name: Android binding (Core.aar)
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Cache Go build cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/go-build
          key: ${{ runner.os }}-gomobile-android-${{ hashFiles('**/go.sum') }}-v1
          restore-keys: |
            ${{ runner.os }}-gomobile-android-

      # GitHub's ubuntu runners ship the Android SDK; ANDROID_NDK_LATEST_HOME
      # points at a preinstalled NDK. gomobile reads ANDROID_NDK_HOME.
      - name: Point gomobile at the runner NDK
        run: echo "ANDROID_NDK_HOME=$ANDROID_NDK_LATEST_HOME" >> "$GITHUB_ENV"

      - name: Install gomobile
        run: |
          go install golang.org/x/mobile/cmd/gomobile@latest
          go install golang.org/x/mobile/cmd/gobind@latest
          gomobile init

      - name: Build Core.aar
        run: |
          mkdir -p android/core
          gomobile bind -target=android -androidapi 21 \
            -o android/core/core.aar ./mobilecore
          ls -la android/core

      - name: Upload AAR
        uses: actions/upload-artifact@v4
        with:
          name: core.aar
          path: android/core/core.aar
          if-no-files-found: error

      # Build and unit-test the Compose app against the AAR produced above
      # (gomobile wrote it to android/core/core.aar, which the Gradle build
      # consumes as a flatDir dependency).
      - name: Build Android app + run unit tests
        run: ./android/gradlew -p android assembleDebug testDebugUnitTest --no-daemon
